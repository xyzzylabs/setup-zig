# Fail `npm install` fast when the active Node version doesn't satisfy
# the `engines` constraint in package.json. Contributors on older Node
# get a clear error instead of an opaque failure mid-test.
engine-strict=true

# Quiet, predictable install output:
# - no funding banner
# - no inline audit report (run `npm audit` explicitly when wanted)
fund=false
audit=false
